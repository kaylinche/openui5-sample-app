_schema-version: '3.2'
ID: extended_flex_demo
version: 0.0.1

build-parameters:
  before-all:
  - builder: custom
    commands: 
    - npm install
    - npm build

modules:
- name: extended_flex_npm
  type: npm
  path: .
